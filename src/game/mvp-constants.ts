/** Pinned MVP economy (docs/prd-mvp.md). */
export const MVP_STARTING_SHELLS = 50;
/** Same as `DEFENSE_BUILD_COST_L1.arc_spine` — kept for older imports/tests. */
export const MVP_ARC_SPINE_BUILD_COST = 30;
